name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  quality:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: lifeos
          POSTGRES_PASSWORD: lifeos
          POSTGRES_DB: lifeos
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U lifeos"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://lifeos:lifeos@localhost:5432/lifeos
      JWT_SECRET: ci-only-secret
      ALLOWED_ORIGINS: "http://localhost:5173"

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm prisma:generate

      - name: Apply migrations
        run: pnpm db:migrate

      - run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: API unit tests
        run: pnpm --filter @lifeos/api test:unit

      - name: API integration tests
        run: pnpm --filter @lifeos/api test:integration

      - name: API E2E + failure scenarios
        run: pnpm --filter @lifeos/api test:e2e

      - name: Web tests
        run: pnpm --filter @lifeos/web test

      - name: i18n key completeness
        run: pnpm --filter @lifeos/web i18n:check

      - name: Full test suite with coverage (fails below thresholds)
        run: pnpm test:coverage

      - name: Dependency audit
        run: pnpm audit --prod

      - run: pnpm build
