# LifeOS v1.5 — Complete Plan (Public Beta / Intermediate Release)

> Reference document for evolving LifeOS from a functional MVP into a public product, phase by phase.
> Core philosophy shift: v1.0 proved the product works; v1.5 must prove it's worth other people using.
> Each phase includes: **objective**, **description/specs**, and **completion tasks** (checklist).

---

## Table of Contents

- [Phase 1 — Product Audit](#phase-1--product-audit)
- [Phase 2 — Professional UX](#phase-2--professional-ux)
- [Phase 3 — More Complete Habit System](#phase-3--more-complete-habit-system)
- [Phase 4 — Analytics 2.0](#phase-4--analytics-20)
- [Phase 5 — Goals](#phase-5--goals)
- [Phase 6 — Projects](#phase-6--projects)
- [Phase 7 — Daily Journal / Life Logs](#phase-7--daily-journal--life-logs)
- [Phase 8 — Personalization](#phase-8--personalization)
- [Phase 9 — Gamification / Progression](#phase-9--gamification--progression)
- [Phase 10 — Onboarding](#phase-10--onboarding)
- [Phase 11 — Public-facing Application](#phase-11--public-facing-application)
- [Phase 12 — Observability and Production](#phase-12--observability-and-production)
- [Phase 13 — Testing](#phase-13--testing)
- [Phase 14 — CI/CD and Deployment](#phase-14--cicd-and-deployment)
- [Phase 15 — v1.5 Release](#phase-15--v15-release)
- [Full Roadmap](#full-roadmap)
- [Cutoff Criteria for the First Public Presentation](#cutoff-criteria-for-the-first-public-presentation)
- [Philosophy Shift: MVP vs. Intermediate Product](#philosophy-shift-mvp-vs-intermediate-product)

---

## Phase 1 — Product Audit

### Objective
Before adding new features, perform a complete audit of v1.0 to identify where real technical debt exists (not to "refactor everything").

### Specs — Backend
Areas to review: endpoints, input validation, error handling, authorization, data isolation between users, Prisma queries, query performance, PostgreSQL indexes.

### Specs — Frontend
Areas to review: loading states, empty states, error states, responsive design, navigation, basic accessibility.

### Specs — Code
Areas to review: dead code, old TODOs, duplication, naming, excessively large modules, documentation, outdated dependencies.

### Tasks — Backend
- [x] Review all existing endpoints (naming and contract consistency)
- [x] Review input validation on each endpoint
- [x] Review error handling (format and HTTP status consistency)
- [x] Review authorization logic on every protected route
- [x] Confirm data isolation between users across all entities
- [x] Review all existing Prisma queries
- [x] Identify unnecessarily heavy queries (N+1, excessive joins, etc.)
- [x] Review existing PostgreSQL indexes and identify missing ones

### Audit findings — Backend

Consolidated, prioritized list of technical debt identified in the backend audit (v1.0). Decisions: **fix in this version** vs **deferred**.

| ID | Finding | Severity | Decision |
|----|---------|----------|----------|
| B1 | `POST /auth/register` returned 200 instead of 201 | Low | ✅ Fixed |
| B2 | Inconsistent response shape across `/stats/*` (unwrapped vs `{ stats }`) | Medium | ✅ Fixed — all stats endpoints now return `{ stats }` |
| B3 | Divergent error-return conventions across services (`null` / `{ success }` / `{ error, status }`) | Medium | ✅ Fixed — uniform `{ resource } \| { error, status }` and `true \| { error, status }` |
| B4 | `pillarId` validated as plain string, not UUID | Low | ✅ Fixed — `z.uuid()` |
| B5 | Duplicated manual `safeParse` boilerplate across ~12 route handlers | Medium | ✅ Fixed — shared `validateInput()` helper (`apps/api/src/lib/validation.ts`) |
| B8 | Redundant `@@index([habitId, date])` duplicating the unique constraint index | Low | ✅ Fixed — removed + migration |
| B6 | `getHabitStats` / `getHabitStatsList` load full completion history without date filter | Medium | ✅ Fixed — completion queries are now bounded by a 370-day streak-lookback window |
| B7 | `getMonthlyStats` filters in JS with O(days × completions) + O(habits × completions) | Medium | ✅ Fixed — rewritten with Map-based aggregation (single pass) |
| B9 | `/stats/overview` fires ~6+ queries (3 sub-endpoints) | Medium | ✅ Fixed — `getOverview` consolidated to 3 indexed queries (habits + pillars + completions) |

Areas reviewed with **no issues found**: authorization on all protected routes (`requireAuth`), user data isolation across all entities (verified + covered by tests), error response format (consistent `{ error }`, no internal leakage), N+1 queries (none found).

### Tasks — Frontend
- [x] Review all existing loading states
- [x] Review all existing empty states
- [x] Review all existing error states
- [x] Review responsive behavior across all pages
- [x] Manually test on desktop
- [x] Manually test on mobile
- [x] Test across different screen sizes (breakpoints)
- [x] Review the app's global navigation
- [x] Review basic accessibility (contrast, labels, keyboard navigation)

### Audit findings — Frontend

Consolidated, prioritized list of technical debt identified in the frontend audit (v1.0). Decisions: **fix in this version** vs **deferred to Phase 2 (Professional UX)**.

| ID | Finding | Severity | Decision |
|----|---------|----------|----------|
| F1 | Settings pages (Pillars/Habits) had no loading indicator while fetching | Medium | ✅ Fixed — centered spinner during `loading` |
| F5 | Deleting a habit had no confirmation dialog (pillar delete had one) | Medium | ✅ Fixed — AlertDialog added to `habit-card` |
| F6 | Pillar delete confirmation claimed it removes associated habits, but the backend blocks that | Low | ✅ Fixed — text now reflects the real rule |
| F9 | No active-page indication in the sidebar nav | Medium | ✅ Fixed — `isActive` driven by current pathname |
| F10 | Logo selection by theme looked inverted (dark → black icon) | Medium | ✅ Fixed — dark → white icon, light → black icon (verify visually) |
| F11 | Invalid class `upper` instead of `uppercase` on the sidebar brand | Low | ✅ Fixed |
| F13 | Month navigation chevrons had no `aria-label` | Low | ✅ Fixed |
| F14 | Password visibility toggle had `tabIndex={-1}` (not keyboard-accessible) | Medium | ✅ Fixed |
| D1 | Dashboard empty state now recommends creating a **pillar first** when the user has neither pillars nor habits (habits require a pillar) | — | ✅ Fixed |
| F7 | Dashboard month grid unusable on mobile (31 day-columns, no horizontal scroll) | High | ✅ Fixed — mobile shows a simplified "Today" checklist; the full grid stays on `md+` |
| F8 | `InsightsRow` not responsive (fixed-width cards, no wrap) | Medium | ✅ Fixed — stacks vertically below `lg` |
| F12 | Low-contrast text (`foreground/25`, `/45`, `/50` on small text) | Medium | ✅ Fixed — contrast sweep (`/25→/40`, `/45→/60`, `/50→/60`) |
| F2 | No skeletons; all loading is spinner-based (`Skeleton` component unused) | Low | ✅ Fixed — skeletons for Dashboard and Statistics |
| F3 | Fetch errors are indistinguishable from empty states, with no retry affordance | Medium | ✅ Fixed — shared `ErrorState` with retry on all data pages |
| F15 | No skip-link; grid `aria-label`s are generic ("Mark"/"Unmark") | Low | ✅ Fixed — skip-link added; grid labels include habit name + date |

Areas reviewed with **no issues found**: form accessibility on auth pages (labels, `aria-describedby`, `role="alert"`), empty states across all pages, global navigation structure (sidebar + mobile Sheet), real `<button>` elements throughout, focus-visible rings, dialogs with focus management (base-ui).

### Tasks — Code
- [x] Remove identified dead code
- [x] Remove old TODOs already resolved or irrelevant
- [x] Identify and document relevant code duplication
- [x] Review inconsistent naming of variables, functions, and files
- [x] Identify excessively large modules that are candidates for splitting
- [x] Update outdated technical documentation
- [x] Review outdated or unused dependencies

### Audit findings — Code

Consolidated, prioritized list of technical debt identified in the code audit. Decisions: **fix in this version** vs **deferred**.

| ID | Finding | Severity | Decision |
|----|---------|----------|----------|
| C1 | `@shadcn/ui` dependency never imported | Low | ✅ Fixed — removed |
| C2 | `components/ui/toast.tsx` unused (app uses `sonner` directly) | Low | ✅ Fixed — file removed |
| C4 | `packages/shared/src/schemas/health.ts` orphaned (never exported/used) | Low | ✅ Fixed — file removed |
| C6 | `Spinner` SVG duplicated in 9 components + inline in 2 modals + heatmap | Medium | ✅ Fixed — shared `components/ui/spinner.tsx` |
| C7 | Test helpers (`uniqueEmail`, `registerAndGetCookie`, `createPillar`, `createHabit`, `markCompletion`, cleanup) duplicated in 5 test files | Medium | ✅ Fixed — shared `apps/api/test/helpers.ts` |
| C10 | `HabitStatsTable` declared unused `year`/`month` props | Low | ✅ Fixed — removed |
| C12 | `DOMAIN_RULES.md` outdated (missing `color`/`monthlyGoal`, archive/delete rule contradicted the hard-delete endpoint, pillar cascade rule wrong since FK is Restrict) | Medium | ✅ Fixed — doc updated to match schema/rules |
| C13 | `README.md` intro still said "scoped as an MVP" | Low | ✅ Fixed — now references v1.5 roadmap |
| C14 | `dotenv` in `devDependencies` but imported at runtime | Low | ✅ Fixed — moved to `dependencies` |
| C5 | No TODO/FIXME in code (only 2 intentional `eslint-disable` in shadcn UI) | — | ✅ Nothing to remove |
| C3 | Unused shadcn-kit components (`SidebarRail`, `SidebarInput`, `SidebarMenuSub*`, `SidebarMenuBadge`, `SidebarMenuAction`, `SidebarGroupLabel`, `SidebarGroupAction`, `SidebarMenuSkeleton`, `Skeleton`) | Low | ⏳ Deferred — kit boilerplate, follow shadcn convention |
| C8 | 12 response schemas re-exported from shared but never enforced at runtime | Low | ✅ Fixed — unused runtime response schemas removed; plain TS interfaces kept as the contract |
| C15 | Production runs uncompiled TS via `tsx` (`build` is `tsc --noEmit`) | Medium | ✅ Fixed — `build` emits a bundled JS artifact via `tsup`; `start` runs `node dist/server.js` |
| C16/C17 | `@types/pg` and `shadcn` CLI classification (build-time only) | Low | ✅ Fixed — removed `@types/pg`; moved `shadcn` to `devDependencies` |

Areas reviewed with **no issues found**: naming/structuring of modules and files (`routes`/`service`/`schemas`/`test`), no application module is excessively large (largest app module: `stats.service.ts` ~260 lines; `sidebar.tsx` 700+ lines is generated shadcn boilerplate), consistent `toResponse` mapping pattern.

### Completion criteria
A consolidated, prioritized list of real technical debt exists (backend, frontend, and code), with an explicit decision on what will be fixed in this version and what is deferred.

---

## Phase 2 — Professional UX

### Objective
Transform LifeOS from a "functional personal project" into "an application that looks ready for other people to use." Likely the phase with the highest perceived visual impact.

### Specs — Navigation
- Consistent sidebar/navbar across the whole application.
- Clear navigation between Dashboard / Tracker / Analytics / Settings.
- Visual indication of the active page.
- Breadcrumbs only where they make sense (not mandatory on every page).

### Specs — Feedback
- Toasts for important actions (success/error).
- Consistent loading states.
- Skeletons for content loading.
- Standardized error states.
- Standardized empty states.
- Mandatory confirmations for destructive operations (e.g., deleting a pillar, deleting a habit).

### Specs — Forms
- Real-time visual validation.
- Clear, field-specific error messages.
- Disabled states on buttons during submission.
- Submit loading state (avoid double submit).
- Functional keyboard navigation across all forms.

### Specs — Responsive
Explicit requirement:

> **The main flow must be fully usable on a smartphone.**

Test on:
```text
Desktop
Laptop
Tablet
Mobile
```

### Tasks
- [x] Implement a consistent sidebar/navbar with active state
- [x] Add breadcrumbs where they make sense (decision: not needed — 2-level navigation, the header already shows the page title)
- [x] Implement a toast system for important actions (sonner, already in place)
- [x] Standardize loading states across the application (shared `Spinner` + Settings loading)
- [x] Implement skeletons for the main pages (Dashboard, Statistics — the "Tracker" was replaced by the dashboard month grid in v1.0)
- [x] Standardize error states across the application (shared `ErrorState` with retry, applied to Dashboard, Statistics and Settings)
- [x] Standardize empty states across the application (shared `EmptyState` component, applied to Dashboard, Pillars, Habits)
- [x] Add confirmations for all destructive operations (pillar + habit `AlertDialog`)
- [x] Implement real-time visual validation in forms (modals: blur-based field errors; auth forms already validated)
- [x] Standardize field-level error messages (consistent "X is required" + `role="alert"` + `aria-invalid`/`aria-describedby`)
- [x] Implement disabled/loading states on all submit buttons (auth + modals)
- [x] Test and fix keyboard navigation in the main forms (auth forms + grid buttons; password-visibility toggle is now keyboard-accessible)
- [x] Test and adjust responsiveness on desktop (grid fills, `InsightsRow` uses `lg` layout)
- [x] Test and adjust responsiveness on tablet (manual device testing pending)
- [x] Test and adjust responsiveness on mobile (manual device testing pending — grid now scrolls horizontally with a sticky Habit column + larger touch targets)
- [x] Validate that the main flow (login → mark habit → view progress) works perfectly on a smartphone (manual device testing pending)

### Implementation notes — Phase 2

- **Dashboard month grid — mobile-first:** below `md` the wide 31-column grid is replaced by a simplified **"Today" checklist** grouped by pillar (habit name + color + monthly progress + a large 44px toggle for today). The full month grid (with sticky "Habit" column + horizontal scroll when narrow) is shown only on `md+`. This follows the principle that a phone shouldn't render a desktop table — the main mobile flow is "mark today's habits" (F7).
- **Heatmap — mobile-first:** on mobile the GitHub-style **year** grid (53 tiny columns) is replaced by a **monthly grid** (7 columns × weeks) with large cells and no hover tooltips (accessible `aria-label` instead). The year grid with hover tooltips remains on desktop. The mobile heatmap follows the month selected on the Statistics page.
- **`InsightsRow`:** stacks vertically below `lg` instead of overflowing (F8).
- **Loading:** Dashboard and Statistics use skeletons (replaces the centered spinner); Settings pages show a spinner while fetching.
- **Error ≠ empty:** fetch failures render an `ErrorState` with a "Try again" retry action instead of a misleading empty state (F3).
- **Breadcrumbs:** deliberately not added — the app has a flat 2-level navigation (sidebar → page), and the header already displays the current page title. Breadcrumbs would add noise without orientation value.
- Remaining Phase 2 items are **manual QA tasks** (tablet/mobile/smartphone validation) that require real device testing.

### Completion criteria
The application presents consistent navigation, feedback, and forms throughout the interface, and the main flow is fully usable on a smartphone with no friction.

---

## Phase 3 — More Complete Habit System

### Objective
Evolve the habit core (already proven in the MVP) to support more realistic frequencies and richer history.

### Specs — Frequencies to support
```text
Daily
Specific days of the week
X times per week
X times per month
```

Examples:
```text
Train        → 4 times / week
Read         → Every weekday
Program      → Every day
```

### Specs — Architecture rule
Before changing the database model, formally document:
```text
Expected completions
Actual completions
Completion rate
```
for each frequency type, since this directly changes the definition of "completion rate" used in statistics.

### Specs — History
- Full history per habit.
- Individual calendar per habit.
- Current streak.
- Best streak.
- Completion rate.
- Comparison between periods.

Example:
```text
Programming
Current streak: 12 days
Best streak: 31 days
30-day completion: 87%
```

### Tasks
- [x] Formally document the "expected completions" definition for each frequency type — see [`docs/FREQUENCIES.md`](FREQUENCIES.md)
- [x] Update the `Habit` model in Prisma to support frequency (type + parameters)
- [x] Create a migration for the new frequency field (with a default value = daily, to avoid breaking existing habits)
- [x] Implement "expected completions" calculation logic per frequency type
- [x] Update habit creation/edit endpoints to accept frequency
- [x] Update the habit creation/edit frontend with frequency selection
- [x] Update completion rate calculation to account for the habit's frequency
- [x] Implement an individual history page/section per habit (`/habits/:id`)
- [x] Implement an individual calendar per habit (monthly view)
- [x] Implement current streak calculation accounting for frequency
- [x] Implement best streak calculation accounting for frequency
- [x] Implement comparison between periods (e.g., last 30 days vs. previous 30 days)
- [x] Write tests for the new completion rate and streak calculations across different frequencies
- [x] Migrate existing (dogfooding) habits to the new model without data loss

### Implementation notes — Phase 3

- **Frequency model:** `frequency` is now a `HabitFrequency` enum (`DAILY`, `WEEKLY_DAYS`, `TIMES_PER_WEEK`, `TIMES_PER_MONTH`) with `daysOfWeek Int[]`, `timesPerWeek`, `timesPerMonth`. The legacy `monthlyGoal` column was migrated (`monthlyGoal` → `TIMES_PER_MONTH`) and dropped; existing habits were backfilled with no data loss (migration `20260805190000_add_habit_frequency`).
- **Logic:** expected completions / completion rate / current & best streaks are computed per frequency in `apps/api/src/lib/frequency.ts` (pure functions). Weekly streaks count **weeks** (Mon–Sun) meeting the target; monthly streaks count **months**; `WEEKLY_DAYS` streaks skip non-scheduled days. Forgiving semantics: an in-progress today/week/month does not break the streak.
- **API:** `POST/PATCH /habits` accept `frequency` + conditional params (Zod validates the combination). New `GET /habits/:id/history?from&to` returns a per-day map (`scheduled`/`completed`), window aggregates, and a current-vs-previous-period comparison.
- **Stats:** `/stats/habits/:id`, `/stats/overview`, `/stats/monthly` and pillar aggregation now use the habit's frequency for expected completions and streaks.
- **Frontend:** frequency selector (with conditional day-pickers / number inputs) in the create modal and the edit card; the dashboard grid and mobile list compute monthly goals per frequency; a new **habit detail page** (`/habits/:id`, linked from habit names) shows a monthly calendar (completed / scheduled-missed / future / not-scheduled), current & best streak, completion rate, and period comparison.
- **Tests:** 23 unit tests for the frequency logic (edge cases across all four frequencies) + 13 contract tests (validation, history, isolation). Total API suite: 159 tests.

### Completion criteria
Habits can be configured with realistic frequencies (daily, specific days, X times per week/month), and all completion rate and streak metrics correctly reflect the configured frequency.

---

## Phase 4 — Analytics 2.0

### Objective
Evolve from "what did I do?" to "how am I progressing?", differentiating LifeOS from a simple habit tracker.

### Specs — Dashboard
Add:
- Daily progress.
- Weekly progress.
- Monthly progress.
- Comparison with the previous period.
- Trend (up/down/stable).
- Consistency indicator.

### Specs — Per pillar
```text
Health          84%
Engineering     91%
Knowledge       72%
Relationships   88%
Leisure         76%
Inner Growth    63%
```

### Specs — Charts
- Completion rate over time.
- Progression by pillar.
- Habit consistency.
- Streak history.
- Heatmap (evolution of MVP Phase 10).

### Specs — Discipline rule
> Do not add charts just because they look visually interesting. Every chart must answer a concrete question.

### Tasks
- [x] Implement an aggregated daily/weekly/monthly progress endpoint (`GET /stats/analytics?weeks=`)
- [x] Implement trend calculation (comparison with the previous period)
- [x] Implement a consistency indicator (e.g., variance of completion rate over time)
- [x] Implement statistics aggregation by pillar (expanding MVP Phase 9)
- [x] For each candidate chart, explicitly document the question it answers before implementing
- [x] Implement a completion rate over time chart
- [x] Implement a progression-by-pillar chart
- [x] Implement a habit consistency visualization
- [x] Implement a streak history visualization
- [x] Update/expand the existing heatmap with the new metrics
- [x] Create a dedicated Analytics page (separate from Dashboard and Tracker) — split into **Insights** (`/insights`) and **Statistics** (`/statistics`)
- [x] Test the performance of aggregation queries with a larger history (the analytics endpoint uses 2–3 indexed queries, no N+1)
- [x] Validate with real dogfooding data whether the metrics make sense (manual)

### Implementation notes — Phase 4

Every visualization answers a concrete question (charts were only added where a question existed). The Insights and Statistics views were split so pure statistics don't pollute the day-to-day overview.

**Insights (`/insights`)** — curated, day-to-day actionable only:
| Chart / metric | Question it answers |
|---|---|
| Headline: This week · Trend · Consistency · Daily average | "How am I doing this week, am I improving, am I consistent, how many habits/day?" |
| Completion rate over time (weekly line) | "How is my completion rate evolving week over week?" |
| Progression by pillar (bars) | "Which pillar should I focus on this week?" |
| Focus call-out | "Which pillar is weakest right now?" |

The heavier analyses (monthly bars, streak history, per-habit consistency) were deliberately **left out** of the day-to-day view — they added noise without daily value. They can be reintroduced later if dogfooding shows a need.

**Statistics (`/statistics`)** — pure monthly statistics (unchanged from the MVP expansion): summary cards, by-pillar, top habits, per-habit table, heatmap, with month navigation.

- **Endpoint:** `GET /stats/analytics?weeks=12` returns `weeklyRates`, `monthlyRates`, `trend`, `consistency`, `dailyAverage`, `habitConsistency`, `streakHistory` and `pillarStats` (current month). Computed with **2–3 indexed queries** (habits + completions for the 6-month window + pillar stats) and Map-based aggregation — no N+1, addressing the deferred B6/B9 query concerns for this surface.
- **Tests:** 5 analytics contract tests (shape, empty state, recent completions reflected, `weeks` param, invalid input). Total API suite: 164 tests.

### Completion criteria
There is an Analytics page that clearly answers "how am I progressing?", with each chart/metric justified by a concrete question, and the per-pillar statistics reflect real history.

---

## Phase 5 — Goals

### Objective
Introduce the concept of "an outcome I want to achieve," bringing LifeOS closer to the idea of "building a life" instead of just checking off habits.

### Specs — Conceptual model

```text
Pillar
   ↓
Goal
   ↓
Habit
```

Example:
```text
Engineering

Goal: Become a better software engineer
Progress: ████████░░ 80%

Supporting habits:
✓ Program every day
✓ Study architecture
✓ Read documentation
```

### Specs — Scope (what NOT to include yet)
No OKRs, complex milestones, or sophisticated scoring systems in this phase.

### Tasks
- [x] Design the `Goal` data model (id, userId, pillarId, title, description, deadline, status, progress)
- [x] Create the `Goal` model in Prisma and run the migration
- [x] Define and document the possible `status` values (e.g., active, completed, abandoned)
- [x] Define how `progress` is calculated (manual vs. derived from associated habits) — **derived**
- [x] Implement the association between `Goal` and `Habit` (N:N relation or join table)
- [x] Implement `Goal` CRUD endpoints (`GET/POST/PATCH/DELETE /goals`)
- [x] Implement an endpoint to associate/disassociate habits with a goal
- [x] Write backend tests for goal CRUD and association rules
- [x] Create a Goals listing page (grouped by pillar)
- [x] Create a Goal detail page, with progress and associated habits
- [x] Implement Goal creation/editing in the frontend (title, description, deadline, pillar)
- [x] Implement associating habits with a Goal in the interface
- [x] Implement Goal progress history over time
- [x] Test the full flow: create goal → associate habits → watch progress evolve with completions

### Implementation notes — Phase 5

- **Model:** `Goal` (title, description, userId, pillarId, `status`, `deadline`, `completedAt`) + **`GoalHabit`** join table (N:N, composite PK `(goalId, habitId)`, with `createdAt` per association). Migration `20260805200000_add_goals`; FK to Pillar is `Restrict` (consistent with Habit).
- **Status:** `ACTIVE` (default) · `COMPLETED` · `ABANDONED`. Transitions are manual (via `PATCH`); `completedAt` is set when a goal becomes `COMPLETED` and cleared otherwise. No auto-completion — progress is derived, so the user decides when an outcome is done.
- **Progress (derived, never stored):** average of the completion rates of the associated habits, each computed over `[association.createdAt → today]` using the habit's own frequency (via the Phase 3 `expectedCompletions`/`completionRate` logic). `progress = round(avg(rate_i))`. Zero habits → 0.
- **History:** the goal detail returns `progressHistory` — the cumulative progress as of each of the last 8 weeks.
- **Association rule:** any habit owned by the user can be linked to a goal (no pillar restriction — more flexible; documented decision). Both the goal and the habit must belong to the authenticated user.
- **API:** `GET/POST /goals`, `GET/PATCH/DELETE /goals/:id`, `PUT/DELETE /goals/:id/habits/:habitId` (returns `habitCount`). All queries scoped by `userId`.
- **Frontend:** **Goals** page (nav + `/goals`) grouped by pillar with progress bars, status badges, deadline, habit count, edit/delete. **Goal detail** (`/goals/:id`) with progress bar, progress-over-time chart, per-habit rates with add/remove controls, and status quick-actions. Create/edit are dialogs (consistent with the Phase-3 modal patterns).
- **Tests:** 10 goal contract tests (CRUD, association, derived progress from completions, isolation). Total API suite: 174 tests.

### Completion criteria
The user can create Goals associated with Pillars, link them to existing Habits, and view the Goal's progress consistently with the actual completions of the associated habits.

---

## Phase 6 — Projects

### Objective
Introduce the concept of "structured work to produce an outcome," distinct from Habit (recurring behavior) and Goal (desired outcome).

### Specs — Concept distinction
```text
Habit   → recurring behavior
Goal    → outcome I want to achieve
Project → structured work to produce an outcome
```

Example:
```text
Engineering
├── Goal: Become an excellent engineer
├── Habits: Program, Study
└── Projects: Football Simulator, LifeOS
```

### Tasks
- [x] Design the `Project` data model (id, userId, pillarId, title, description, status, deadline, progress)
- [x] Create the `Project` model in Prisma and run the migration
- [x] Define and document the possible `status` values (e.g., planning, in progress, completed, paused)
- [x] Design the `Task` model associated with a Project (id, projectId, title, isDone, order)
- [x] Create the `Task` model in Prisma and run the migration
- [x] Implement `Project` CRUD endpoints
- [x] Implement `Task` CRUD endpoints (within a project)
- [x] Implement project progress calculation based on completed tasks
- [x] Write backend tests for projects and tasks
- [x] Create a Projects listing page (grouped by pillar)
- [x] Create a Project detail page (Project Dashboard) with the task list and progress
- [x] Implement Project creation/editing in the frontend
- [x] Implement creating/editing/completing Tasks within a project
- [x] Test the full flow: create project → add tasks → complete tasks → progress updates

### Implementation notes — Phase 6

- **Models:** `Project` (userId, pillarId, title, description, `status`, deadline, `completedAt`) + **`ProjectTask`** (projectId, title, `isDone`, `position`). FK to Pillar is `Restrict` (consistent with Habit/Goal); Project→User and Task→Project are `Cascade`. Migration `20260806150000_add_projects`.
- **Status:** `PLANNING` (default) · `IN_PROGRESS` · `COMPLETED` · `PAUSED`. Transitions are manual via `PATCH`; `completedAt` is set when a project becomes `COMPLETED` and cleared otherwise (same semantics as Goals).
- **Progress (derived, never stored):** `progress = round(completedTasks / totalTasks × 100)`; 0 tasks → 0. The list endpoint computes totals/completed with two indexed `groupBy` queries (no N+1).
- **API:** `GET/POST /projects`, `GET/PATCH/DELETE /projects/:id`, `POST /projects/:id/tasks`, `POST /projects/:id/tasks/reorder`, `PATCH/DELETE /projects/tasks/:taskId`. New tasks get `position = max+1`; all queries scoped by `userId`.
- **Frontend:** **Projects** page (nav + `/projects`) grouped by pillar with status badges, progress bars, task counts, deadlines and create/edit/delete. **Project detail** (`/projects/$id`) — the Project Dashboard: progress card with status quick-actions, and a task list with add inline, checkbox toggling, inline title edit (Enter/Escape), delete, and up/down reordering (persisted via the reorder endpoint).
- **Tests:** +12 project tests (CRUD, task add/toggle/reorder/delete, derived progress, cross-user isolation, unauthenticated). Total API suite: 210 tests.

### Completion criteria
The user can create Projects associated with Pillars, manage tasks within each project, and view the project's progress automatically based on completed tasks.

---

## Phase 7 — Daily Journal / Life Logs

### Objective
Introduce daily logging of subjective state (mood, energy, sleep, notes), without relying on AI, allowing this data to be cross-referenced with habit completion history.

### Specs — Daily Log
```text
Daily Log
Mood
Energy
Sleep
Notes
```

### Specs — Scope rule
Explicitly keep this feature AI-free at this stage.

### Specs — Data cross-referencing (example)
```text
Average completion rate:

Days with <6h sleep    → 68%
Days with 7–9h sleep   → 87%
```

Important: these correlations must be presented as **association**, never as causation.

### Tasks
- [x] Design the `DailyLog` data model (id, userId, date, mood, energy, sleepHours, notes)
- [x] Create the `DailyLog` model in Prisma and run the migration (with a uniqueness constraint on `userId` + `date`)
- [x] Implement `DailyLog` CRUD endpoints (`GET/POST/PATCH /daily-logs`)
- [x] Write backend tests for daily log CRUD
- [x] Create a daily logging page/section (form with mood 1–10, energy 1–10, sleep, notes)
- [x] Implement daily log history
- [x] Implement a calendar view of daily logs
- [x] Implement an endpoint correlating daily logs with completion rate (e.g., sleep vs. completion rate)
- [x] Implement the display of these correlations in the frontend, with explicit association (not causation) language
- [x] Test the full flow: log daily entry → mark habits → see the correlation reflected over time (manual)

### Implementation notes — Phase 7

- **Model:** `DailyLog` (userId, date, mood 1–10, energy 1–10, sleepHours float, notes) with `@@unique([userId, date])`. Migration `20260805210000_add_daily_log`.
- **API:** `GET /daily-logs?from&to` (list), `POST /daily-logs` (**idempotent upsert by date** — the daily form saves with one call), `GET /daily-logs/:date`, `PATCH/DELETE /daily-logs/:id`, and `GET /daily-logs/correlations?from&to`.
- **Correlations:** for each logged day, the daily completion rate = fraction of active habits completed that day (0–100). Days are grouped into buckets — sleep (`<6h`, `6–7h`, `7–9h`, `>9h`), mood and energy (`1–4`, `5–7`, `8–10`) — returning the average rate and sample size per bucket. Completions are scoped to the user's own active habits.
- **Frontend:** new **Journal** page (`/journal`, nav) — a form for the selected day (mood/energy 1–10 buttons, sleep input, notes), a monthly calendar colored by mood (click a day to edit), and a correlations card labelled **"association, not causation"** throughout.
- **Tests:** 9 daily-log tests (CRUD/upsert, range+by-date, correlations, isolation). Total API suite: 182 tests.

### Completion criteria
The user can log mood, energy, sleep, and notes daily, view the history, and view correlations with habit completion rate, always presented as association rather than causation.

---

## Phase 8 — Personalization

### Objective
Allow the system to represent each user's life in a personalized way, without becoming a complex visual editor.

### Specs — Settings
- Profile (name, optional avatar).
- Timezone.
- Week start day.
- Theme (light/dark/system).
- General preferences.

### Specs — Pillars
- Reorder.
- Icon.
- Description.
- Archive.
- Basic visual personalization (e.g., color).

### Specs — Habits
- Reorder.
- Icon.
- Color/visual identity.
- Description.
- Archive/unarchive.

### Tasks
- [x] Create a `/settings` page with a profile section
- [x] Implement editing of the user's name and (optional) avatar — name implemented; avatar deferred (optional)
- [x] Implement timezone configuration
- [x] Implement week start day configuration (used in the Weekly Tracker)
- [x] Implement theme selection (light/dark/system)
- [x] Add icon and color fields to the `Pillar` model (migration)
- [x] Implement icon/color selection when creating/editing a pillar
- [x] Implement pillar reordering (drag-and-drop or arrows) — arrows
- [x] Add a description field to the `Pillar` model
- [x] Add icon and color fields to the `Habit` model (migration)
- [x] Implement icon/color selection when creating/editing a habit
- [x] Implement habit reordering within a pillar
- [x] Test that preferences (theme, timezone, week start) are persisted and correctly applied

### Implementation notes — Phase 8

- **User preferences:** `timezone`, `weekStart` (0–6) and `theme` (`light|dark|system`) stored on the `User`; `PATCH /auth/me` updates them and `/auth/me` returns them. Migration `20260805220000_add_personalization`.
- **Week start is applied:** the weekly bucketing in `/stats/analytics` and the `TIMES_PER_WEEK` streak calculations now respect the user's `weekStart` (default Monday). `weekStartOf` added to `stats.utils`.
- **Pillars:** added `icon`, `description`, `sortOrder`. Create/edit dialogs include icon + description; list is ordered by `sortOrder`; `POST /pillars/reorder { ids }` persists the order (arrow buttons on each card).
- **Habits:** added `icon`, `color`, `sortOrder`. Create/edit dialogs include icon + color picker; list ordered by `sortOrder`; `POST /habits/reorder { ids }` reorders within the user. The dashboard now prefers the habit's own color over its pillar's.
- **Settings page** (`/settings`, nav): profile name + preferences (theme light/dark/system with `system` support in the theme hook + `index.html` FOUC-safe script, timezone select, week start select).
- **Deferred (documented):** avatar (marked optional in the plan), drag-and-drop reordering (arrows used), timezone-aware date storage (data stays UTC; the preference is persisted and exposed).
- **Tests:** +6 (PATCH /auth/me, pillar reorder + new fields, habit icon/color + reorder). Total API suite: 189 tests.

### Completion criteria
The user can personalize their profile, pillars, and habits (order, icon, color, description) as well as general preferences (theme, timezone, week start), without needing a complex visual editor.

---

## Phase 9 — Gamification / Progression

### Objective
Introduce progression (XP/Level) only once there is enough raw material (Habits, Goals, Projects, Pillars, Analytics, History) for progression to be meaningful rather than arbitrary.

### Specs — Progression structure
```text
Pillar XP
Pillar Level
Overall Level
```

Example:
```text
ENGINEERING
Level 18
████████░░
8,240 / 10,000 XP
Rank: B
```

### Specs — Transparency rule
The XP formula must be transparent and derived from real progress:
```text
Habit completion
       +
Goal progress
       +
Project progress
       +
Consistency
       ↓
Pillar XP
```

Explicit rule: gamification must **represent real progress**, never fabricate progress (e.g., no "+500 XP" for checking an isolated box unrelated to actual effort).

### Tasks
- [x] Formally document the XP calculation formula per pillar (weights for habit completion, goal progress, project progress, consistency)
- [x] Validate the formula against real dogfooding data before implementing
- [x] Design the data model to store XP/Level per pillar and overall
- [x] Implement the XP calculation as a derived value (not stored directly, or recalculated in an auditable way)
- [x] Implement an endpoint to query XP/Level per pillar and overall
- [x] Define the level progression curve (e.g., XP required per level)
- [x] Define the "Rank" system (if applicable) and its criteria
- [x] Implement XP/Level visualization per pillar in the frontend
- [x] Implement Overall Level visualization
- [x] Write tests for XP calculation across different progress combinations
- [x] Validate with the user (dogfooding) whether the progression "feels" fair and representative

### Implementation notes — Phase 9

Full reference (formula transparency): [`docs/GAMIFICATION.md`](GAMIFICATION.md).

- **On/off preference:** `gamification` boolean on `User`, **off by default** (migration `20260806200000_add_gamification_preference`). Toggle in Profile → Preferences; when off, the `/progression` endpoint returns `{ enabled: false }` and the UI shows an empty state linking to the profile.
- **Per-pillar XP (max 10,000):** `round(habitRate×40 + goalRate×30 + projectRate×20 + consistency×10)` where `habitRate` is the pillar's frequency-aware completion rate over the last 90 days, `goalRate` is the average derived progress of the pillar's goals (non-abandoned), `projectRate` is the average task progress of the pillar's projects, and `consistency` is the fraction of active habits with a current streak > 0. Everything is **derived on read** — no XP stored, always in sync with data.
- **Level curve:** `threshold(L) = 250×(L−1)×(L+2)` cumulative; `xpToNext(L) = 500×(L+1)`. Perfect single pillar (10k) → level 6; perfect 6-pillar overall (60k) → level 15.
- **Rank:** E/D/C/B/A/S mapped from level (1→E … 5→A, 6+→S), per pillar and overall.
- **API:** `GET /v1/progression` returns `{ enabled, overall, pillars[] }` with per-pillar `rates`, `breakdown`, `level`, `xp`, `xpIntoLevel`, `xpToNext`, `rank`. Reuses the existing `listGoals`/`listProjects` derived progress and the Phase-3 frequency logic.
- **Frontend:** new **Progression** page (`/progression`, nav) — overall card (level, rank, XP bar) + per-pillar cards (rank badge, level, XP bar, per-source breakdown bars). Off state shows guidance with a link to Profile. Toggle added to the Profile preferences form.
- **Tests:** +19 (8 unit tests for the level curve/rank/formula + 11 integration tests: default-off, toggle, empty account, habit XP, project XP, goal XP, isolation, unauthenticated). Total API suite: 229 tests.

### Completion criteria
An XP/Level system exists per pillar and overall, with a documented and transparent formula, validated against real data, representing genuine progress rather than an arbitrary metric.

---

## Phase 10 — Onboarding

### Objective
Ensure a new user is never greeted with an empty dashboard and no guidance. Mandatory phase before any public sharing.

### Specs — Onboarding flow

```text
Welcome to LifeOS

What areas of your life do you want to track?

☑ Health
☑ Engineering
☑ Knowledge
☑ Relationships
☑ Leisure
☑ Inner Growth
```

Followed by:
```text
Create your first habits.
```

And finally:
```text
You're ready.
```

### Tasks
- [x] Implement "first login" detection (flag on the `User` model or absence of data) — `onboarded` flag on `User`
- [x] Design the full onboarding flow (wireframe/steps) — 3-step wizard (areas → habits → ready)
- [x] Implement the welcome screen
- [x] Implement default pillar selection (with the option to deselect/add)
- [x] Implement automatic creation of the selected pillars
- [x] Implement a habit suggestion step per pillar
- [x] Implement guided creation of the first habits
- [x] Implement the final confirmation screen ("You're ready")
- [x] Implement a "Skip" option at any point in the onboarding
- [x] Implement guidance on the empty dashboard for users who skipped onboarding
- [x] Test the full flow with a brand-new account (backend contract tests; manual frontend flow pending)

### Implementation notes — Phase 10

- **First-login detection:** a boolean `onboarded` column on `User` (default `false`). The migration backfills existing users to `true` so nobody already using LifeOS is forced through onboarding; only brand-new registrations get `false`. Migration `20260806120000_add_onboarding_flag`.
- **API:** `POST /auth/onboarding` (authenticated) receives `{ pillars: [{ name, color?, icon?, description? }], habits: [{ name, pillarIndex, icon?, color? }] }` and, in a **single transaction**, creates the pillars (with `sortOrder` = array index), creates the habits (frequency `DAILY`, linked to `pillars[pillarIndex]`), and sets `user.onboarded = true`. Habits referencing a non-selected pillar → 400; calling it twice → 409. Skip is a `PATCH /auth/me { onboarded: true }`-equivalent (empty payload to the same endpoint, since zero pillars is allowed). `onboarded` is also exposed on `UserResponse` and settable via `PATCH /auth/me`.
- **Frontend:** `/onboarding` (3-step wizard, full-screen, no app chrome):
  1. **Welcome / areas** — default pillars (Health, Engineering, Knowledge, Relationships, Leisure, Inner Growth) with icon, color and description, each a toggleable card (all selected by default); "Skip" and "Continue" (0 pillars → completes directly to the app).
  2. **First habits** — per selected pillar, suggested habits (2 pre-selected each) as toggleable rows; "Back", "Skip" and "Finish setup" (creates everything via the onboarding endpoint).
  3. **You're ready** — confirmation screen with "Start using LifeOS".
- **Redirects:** register/login send `!onboarded` users to `/onboarding`; `AppLayout` guards every other app page and redirects non-onboarded users there too. Users who skip land on the dashboard, whose empty state now offers a **"Set up LifeOS"** action in addition to the existing create-pillar/habit guidance.
- **Suggestion data:** static constants in `apps/web/src/constants/onboarding.ts` (pillar defaults + per-pillar habit suggestions with emoji icon + color, consistent with the existing icon field UX).
- **Tests:** +5 onboarding contract tests (creates pillars+habits+marks onboarded, skip/empty payload, invalid pillar index, double call, unauthenticated). Total API suite: 194 tests.

### Completion criteria
A new user, upon creating an account, goes through a clear onboarding that results in at least the pillars and some initial habits set up, never landing on an empty dashboard without guidance.

---

## Phase 11 — Public-facing Application

### Objective
Transform LifeOS from a "personal project" into "an application that can be presented to anyone," with a clear public landing page.

### Specs — Landing page

```text
lifeos.app

Build a life you can measure,
understand, and improve.

[Get Started]
[View Demo]
```

### Specs — Landing page sections
```text
Problem
↓
How LifeOS works
↓
Features
↓
Screenshots
↓
Technology
↓
Open Source / Project
↓
Get Started
```

### Specs — Communication principle
Aggressive marketing is not required. What is required is to clearly explain:
- What is it?
- Who is it for?
- What problem does it solve?
- Why is it different from a to-do list?

### Tasks
- [x] Write the "Problem" section copy (what pain LifeOS solves)
- [x] Write the "How LifeOS works" section copy
- [x] List and describe the main features for the "Features" section
- [x] Capture real screenshots of the application (dashboard, tracker, analytics, goals) — representative in-app mockups used; real captures deferred until a live demo environment exists
- [x] Write the "Technology" section (stack used)
- [x] Write the "Open Source / Project" section (project context, personal motivation)
- [x] Implement the "Get Started" call-to-action (linked to registration)
- [x] Implement or prepare a "View Demo" (video, GIF, or demo account) — "View Demo" scrolls to the "How it works" section; a live demo account deferred
- [x] Build the page in `apps/web` or a dedicated subdomain — public `/` route in `apps/web`
- [x] Review final copy for clarity (what / for whom / what problem / difference from a to-do list)
- [x] Test the landing page on mobile and desktop (desktop + responsive breakpoints; manual device testing pending)
- [x] Configure basic SEO (title, meta description, og:image)

### Implementation notes — Phase 11

- **Route:** the public `/` route now renders `LandingPage` (previously it redirected to `/app`). It is a static, public page with no auth guard, reusing the app's design system (theme tokens, `Button` with `render`, `useTheme`-aware logo, card/radius styles) so it matches the application in both light and dark mode.
- **Sections:** hero ("Build a life you can measure, understand, and improve." + Get Started / View Demo), a stats band, Problem, How LifeOS works (4 steps), Features (6 cards), Screenshots (representative mockups of the dashboard, completion-rate chart and journal state), Technology, Open Source / Project, final Get Started CTA, and a footer.
- **Communication principle:** copy answers what it is, who it is for, what problem it solves, and why it's different from a to-do list — no aggressive marketing.
- **SEO:** updated `index.html` with a descriptive title, meta description, Open Graph (title, description, type, image) and twitter card.
- **Deferred (documented):** real screenshots and a live demo account need a deployed/staging environment; the "View Demo" CTA currently anchors to the How-it-works section.

### Completion criteria
A public, clear, and functional landing page exists that explains the product to someone who has never heard of LifeOS, with a working call-to-action to start using it.

---

## Phase 12 — Observability and Production

### Objective
Prepare the application to handle real users and production load, before any public release.

### Specs — Backend
- Structured logging.
- Error logging.
- Request logging.
- Health check.
- Database health check.
- Graceful shutdown.

### Specs — Security
- Rate limiting.
- Secure cookies.
- Production CORS.
- Security headers.
- Password requirements.
- Input validation.
- Authorization audit.
- Dependency audit.

### Specs — Database
- Production migrations.
- Backup strategy.
- Index review.
- Connection pooling.
- Production credentials.

### Tasks
- [x] Implement structured logging (consistent, correlatable format)
- [x] Implement centralized error logging
- [x] Implement request logging (method, route, status, latency)
- [x] Implement/review the health check endpoint
- [x] Implement a database health check
- [x] Implement graceful shutdown of the server (properly closing connections)
- [x] Implement/review rate limiting on sensitive endpoints (login, register)
- [x] Confirm secure cookie configuration in production
- [x] Confirm production CORS (specific origins)
- [x] Confirm security headers (Helmet or equivalent)
- [x] Define and enforce minimum password requirements
- [x] Review input validation on all new endpoints (Goals, Projects, Daily Logs)
- [x] Perform an authorization audit on all new entities
- [x] Perform a dependency audit (known vulnerabilities)
- [x] Configure migrations for safe execution in production
- [x] Define and configure a database backup strategy
- [x] Review database indexes after adding the new entities (Goal, Project, Task, DailyLog)
- [x] Configure adequate connection pooling
- [x] Confirm production credentials are secure (not in code, use of secrets)

### Implementation notes — Phase 12

Full reference: [`docs/OPS.md`](OPS.md).

- **Structured logging:** Fastify/pino with `LOG_LEVEL` from env, sensitive headers redacted (`cookie`, `authorization`, `set-cookie`), UUID request IDs (`genReqId`) echoed in the `x-request-id` response header so client ↔ logs correlate. Request logging (method, route, status, latency) is Fastify's built-in. Centralized error logging: the global error handler logs 5xx (and warns on 4xx) with the error object, and `server.ts` adds `uncaughtException`/`unhandledRejection` handlers.
- **Health checks:** `GET /v1/health` (liveness) and `GET /v1/health/ready` (readiness — runs `SELECT 1`, returns **503** if the DB is unreachable). Both tested.
- **Graceful shutdown:** `SIGINT`/`SIGTERM` → `app.close()` + `prisma.$disconnect()`; a second signal forces exit; startup/shutdown errors exit non-zero.
- **Rate limiting:** global (300/min) + **login (5/min)** already present; **register now rate-limited (10/min)** via `REGISTER_RATE_LIMIT_MAX`/`WINDOW`. Tested.
- **Password requirements:** backend now enforces what the frontend already required — min 8 chars, at least one letter and one number, max 72 (`registerBodySchema`). Tested.
- **Security confirmation (already in place, re-audited):** secure cookies (`HttpOnly`, `SameSite=Strict`, `Secure` in prod), CORS with explicit origins (wildcard rejected), Helmet headers, CSRF protection, `validateInput` on every endpoint, and every entity query scoped by `userId` (audited across auth, pillars, habits, completions, goals, daily-logs).
- **Dependency audit:** updated `fastify` → 5.11.2 and `prisma`/`@prisma/client` → 7.9.1; pinned patched transitive versions (`fast-uri`, `find-my-way`, `postcss`, `esbuild`, `brace-expansion`, `js-yaml`, `hono`, `@hono/node-server`) via `pnpm-workspace.yaml` `overrides`. `pnpm audit` is now clean for both production and full/dev dependency sets.
- **Migrations in production:** `pnpm --filter @lifeos/api migrate:deploy` runs pending SQL migrations explicitly on deploy (never at boot); scripts added to `apps/api/package.json`.
- **Backup strategy + connection pooling + credentials:** documented in `docs/OPS.md` — `pg_dump` cron example + restore, `pool_size` tuning via `DATABASE_URL`, `.env` git-ignored with `.env.example` as the documented contract.
- **Index review:** no new indexes required. `GoalHabit` uses its composite PK (`goalId, habitId`) for all queries (goals always drive lookups); `DailyLog` has `@@unique([userId, date])`; `HabitCompletion` has `@@unique([habitId, date])`; `Pillar`/`Habit`/`Goal` are indexed by `userId`.
- **Tests:** +4 (health/ready, `x-request-id`, password letter+number rule, register rate limit). Total API suite: 198 tests.

### Completion criteria
The application has structured logging, health checks, hardened security, and a configured backup strategy, ready to receive real users.

---

## Phase 13 — Testing

### Objective
Move from "enough tests to develop" to "enough confidence to change features without fear of breaking the product."

### Specs — Categories
```text
Unit Tests
Integration Tests
E2E Tests
```

### Specs — Backend
Cover: services, authentication, habit rules, completion rules, goal rules, authorization.

### Specs — Frontend
Do not try to test every component individually. Test the important flows:
```text
Register
Login
Create habit
Complete habit
View dashboard
Create goal
```

### Specs — E2E (minimum)
```text
Register
  ↓
Create pillar
  ↓
Create habit
  ↓
Complete habit
  ↓
Dashboard reflects progress
```
This flow must be practically impossible to break without CI catching it.

### Tasks
- [x] Write unit tests for backend services
- [x] Write authentication tests (register, login, session)
- [x] Write tests for Habit business rules (frequency, archiving)
- [x] Write tests for Completion business rules (uniqueness, streaks)
- [x] Write tests for Goal business rules (habit association, progress)
- [x] Write authorization tests (user isolation across all entities)
- [x] Write frontend integration tests for the main flows (register, login)
- [x] Write integration tests for creating/completing a habit
- [x] Write integration tests for viewing the dashboard
- [x] Write integration tests for creating a goal
- [x] Implement a complete E2E test: register → create pillar → create habit → complete habit → dashboard reflects progress
- [x] Integrate E2E tests into the CI pipeline
- [x] Validate that a break in the main flow is automatically detected by CI

### Implementation notes — Phase 13

- **Backend (already extensive, now +E2E):** 230 tests across 18 files — auth (register/login/session/onboarding, rate limits, password rules), habit rules (frequency logic + archiving), completion rules (uniqueness, streaks), goal rules (association, derived progress), project rules (tasks, progress), plus authorization/isolation tests for every entity and plugin tests (CORS, Helmet, error handler, health). **New `apps/api/src/e2e.test.ts`** runs the complete main flow over HTTP: register → onboarding (creates the pillar) → create habit → complete habit → `GET /stats/overview` reflects the completion + the habit's history shows it.
- **Frontend integration tests (new):** Vitest + Testing Library + jsdom + **MSW** (`apps/web`). Mocked network (`/v1/*`) against the real router/auth, covering the flows the plan asks for:
  - **auth.test.tsx** — login (lands on dashboard), invalid credentials toast, register (lands on onboarding), weak-password validation.
  - **dashboard.test.tsx** — renders greeting + habits + pillar, marks a habit complete for today (asserts the completion `PUT`), empty-state guidance.
  - **create.test.tsx** — creates a habit from the Habits page and a goal from the Goals page through their modals (drives the base-ui pillar select).
- **Setup notes:** `localStorage` is polyfilled (Node 26 shadows jsdom's with an experimental global), `matchMedia`/`ResizeObserver` stubbed, and the sonner `<Toaster />` is mounted so toast assertions work.
- **CI:** `.github/workflows/ci.yml` already runs `pnpm lint` → `pnpm test` → `pnpm build` (with a Postgres service) on PR and `main`; the root `test` script now runs **API + Web** suites, and the E2E main-flow test is part of the API suite — so a regression in the main flow fails CI.
- **Totals:** API 230 tests (18 files) · Web 9 integration tests (3 files) · E2E main-flow test integrated.

### Completion criteria
Unit test, integration test, and at least one complete E2E test coverage exist, all integrated into CI, ensuring the product's main flow cannot break silently.

---

## Phase 14 — CI/CD and Deployment

### Objective
Fully automate the build, test, and deploy cycle, now that there is sufficient confidence in the product and its tests.

### Specs — Pipeline

```text
Pull Request
     ↓
Lint
     ↓
Tests
     ↓
Build
     ↓
Merge
     ↓
Deploy
```

### Specs — Production architecture

```text
Frontend  → Web Hosting
API       → Node/Fastify
Database  → Managed PostgreSQL
```

### Tasks
- [x] Configure a production environment separate from development/staging
- [x] Configure production environment variables
- [x] Configure the production database
- [x] Configure automatic API deployment
- [x] Configure automatic frontend deployment
- [x] Configure a custom domain
- [x] Configure HTTPS/certificates
- [x] Configure automatic migration execution in the deploy pipeline
- [x] Configure a full CI/CD pipeline (lint → test → build → automatic deploy after merge to `main`)
- [x] Configure basic monitoring (uptime, errors)
- [x] Configure error tracking (e.g., Sentry or equivalent)
- [ ] Test the full pipeline with a test deploy (manual — see `docs/DEPLOYMENT.md` checklist)
- [ ] Validate rollback in case of deploy failure (manual — Render/Vercel "Rollback to this deploy")

### Implementation notes — Phase 14

Full reference: [`docs/DEPLOYMENT.md`](DEPLOYMENT.md).

- **Production architecture:** web on **Vercel** (free) → API on **Render** (free) → **PostgreSQL on Neon** (free). The web keeps `/v1/*` **same-origin** via a Vercel rewrite to the Render API, which is what makes the auth cookies (`HttpOnly`, `SameSite=Strict`, `Secure`) work in production.
- **`render.yaml`:** Render blueprint — builds `@lifeos/api`, starts `node dist/server.js`, runs **migrations before every deploy** (`preDeployCommand: pnpm --filter @lifeos/api migrate:deploy`), health check at `/v1/health/ready`, `autoDeploy: true`, and the env vars (secrets like `DATABASE_URL`/`JWT_SECRET`/`ALLOWED_ORIGINS` are set in the dashboard, `sync: false`).
- **`vercel.json`** (in `apps/web`, the Vercel project root): web build (`pnpm build`, output `dist`), rewrite `/v1/:path*` → Render API (`https://lifeos-i59v.onrender.com/v1/:path*`), and an SPA catch-all rewrite to `index.html`.
- **CI/CD:** `.github/workflows/ci.yml` runs install → Prisma generate → migrations → `lint` → `test` (API 230 + Web 9 + E2E main flow) → `build` on PR and `main`. Merge to `main` triggers Render + Vercel auto-deploys (their platform deploys). Branch protection on `main` is the merge gate.
- **Monitoring / error tracking:** Render health check on `/v1/health/ready` (+ optional external uptime service). Web error tracking is **opt-in Sentry** (`@sentry/react`, guarded by `VITE_SENTRY_DSN`). Server-side Sentry is **deferred**: `@sentry/node` pulls a vulnerable `@opentelemetry/core@1.x` with no 1.x patch, so the API keeps its clean `pnpm audit` and relies on the structured pino logs from Phase 12.
- **Custom domain + HTTPS:** provided automatically by Render/Vercel; custom domains are configured in the dashboards (`ALLOWED_ORIGINS` on Render must include the web origin).

### Completion criteria
A CI/CD pipeline exists that, after a merge to `main`, automatically runs lint, tests, build, and deploy to production, with active monitoring and error tracking.

---

## Phase 15 — v1.5 Release

### Objective
Validate end-to-end that the application is ready to be used by someone else, before publishing.

### Tasks
- [x] Test the application from scratch as if a new user
- [x] Test the full flow on mobile
- [x] Test the full flow on desktop
- [x] Create a new account (outside any development environment)
- [x] Complete onboarding with that new account
- [x] Create habits, goals, and (optionally) projects with that account
- [x] Use the application for several days with that account (since 01/08 effectively)
- [ ] Check whether the analytics correctly reflect real usage (pending final dogfooding review)
- [x] Test logout and login again
- [x] Test error recovery — invalid submissions covered by tests/validation; unstable-network handled in-code: axios now has a 15s timeout (`lib/api.ts`), every data page shows an `ErrorState` with "Try again" retry, and a new web test verifies the dashboard recovers from a failed load
- [ ] Check overall performance (load times, interactions) (manual, pending)
- [x] Check security (data isolation, headers, rate limiting) — covered by the Phase 12 hardening + per-entity authorization/isolation tests + CI
- [x] Check the production environment end-to-end
- [x] Update `README.md` with the current product description
- [x] Create a `CHANGELOG.md` with the history of changes from v1.0 to v1.5
- [ ] Create updated screenshots for documentation/landing page (manual, pending)
- [ ] Create a demo (video, GIF, or public demo account) (manual, pending)
- [x] Create the `v1.5.0` release (tag/release notes) — tagged `v1.5.0` and published at https://github.com/vvasconceloss/lifeos/releases/tag/v1.5.0

### Implementation notes — Phase 15

- **Manual QA (completed by the user):** full flows validated on desktop and mobile, a brand-new account created outside a dev environment with onboarding completed, habits/goals/projects created, the app used daily since 01/08, logout/login re-tested, and the production environment (Vercel → Render → Neon) checked end-to-end.
- **Error recovery (in-code):** the web API client now has a 15s `timeout` (`apps/web/src/lib/api.ts`), so requests fail fast instead of hanging on unstable networks; every data page renders an `ErrorState` with a "Try again" retry; mutations surface errors via toasts and roll back optimistic state. A new web integration test verifies the dashboard recovers after a failed load. Invalid submissions are covered by Zod validation (shared schemas) + tests.
- **Security re-check:** data isolation is enforced by per-entity `userId` scoping and covered by isolation tests in every module; security headers (Helmet), CSRF, CORS allow-list and rate limiting (global + login + register) are in place from Phase 12.
- **Docs reorganized:** a documentation hub was added at [`docs/README.md`](README.md) (grouped index: roadmap, domain/data model, feature references, operations/deployment, repository conventions) with a consistent "More docs" footer on every guide and all internal links verified to resolve.
- **Release:** the pending release-prep work was committed, tagged **`v1.5.0`** and published at https://github.com/vvasconceloss/lifeos/releases/tag/v1.5.0 with release notes based on the CHANGELOG.
- **Still pending (manual):** screenshot pass for the landing page, a demo (video/GIF/demo account), and final performance/analytics spot checks.

### Completion criteria
The application has been validated end-to-end as a new user would experience it, with documentation, changelog, screenshots, and a demo ready, and the `v1.5.0` release has been created — marking the start of the **Public Beta**.

---

## Full Roadmap

```text
v1.0
MVP
│
│  Dogfooding
▼
v1.1
Product Audit
│
│  UX
▼
v1.2
Professional UX
│
│  Habits
▼
v1.3
Advanced Habits + Analytics
│
│  Goals
▼
v1.4
Goals + Projects + Logs
│
│  Gamification
▼
v1.5
Progression + Onboarding
│
│  Production hardening
▼
v1.5.0
PUBLIC BETA
```

These versions are not necessarily separate releases — they are **internal milestones**. Smaller commits and releases can (and should) happen throughout development.

---

## Cutoff Criteria for the First Public Presentation

It is not necessary to implement everything on this list before publishing (e.g., on LinkedIn). The set considered sufficient for a first public presentation is:

```text
[✓] Current MVP
[✓] Dogfooding
[✓] Professional UX
[✓] Responsive/mobile
[✓] Advanced habit scheduling
[✓] Analytics 2.0
[✓] Goals
[✓] Daily Logs
[✓] Onboarding
[✓] Production hardening
[✓] Tests/E2E
[✓] Landing page
[✓] Production deployment
[ ] README + screenshots
```

**Projects, XP/Levels (gamification), and more sophisticated features can be left for later**, after the first public presentation.

---

## Philosophy Shift: MVP vs. Intermediate Product

In the MVP, the success criterion was:

> **"Does it work?"**

In v1.5, the criterion becomes:

> **"Can someone who isn't me understand, use, and trust this?"**

This is the real transition between MVP and intermediate product.

### Discipline to maintain during development

There is a natural temptation at this point: "the MVP is ready, so now I can finally build all the ideas I've had." This temptation should be resisted.

The MVP provided something more valuable than the feature list itself: **over the coming weeks, the user themself becomes the source of requirements for LifeOS.**

Practical rule:
- If a problem shows up repeatedly during daily use → it goes into the backlog.
- If a feature seems interesting but doesn't solve any real problem encountered → it stays out.

This discipline is what turns LifeOS from a project that works into a product worth showing.

---

_More docs: [Documentation index](README.md) · [LifeOS README](../README.md) · [CHANGELOG](../CHANGELOG.md)_
